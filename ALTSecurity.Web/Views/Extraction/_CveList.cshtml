@using ALTSecurity.Web.Utility;
@model  List<ALTSecurity.Web.Models.CveRes>
@Html.ModalWindow("cveWindow", "Версії програмного забезпечення", @<text>
    <table id="cveTable" class="dataTable table-bordered table-striped" style="width:100%" role="grid"></table>
    @Html.DataTables(new DataTables("cveTable")
                   {
                       Dom = "rtlp",
                       Columns = new List<DataTableColumn>
                       {
                       new DataTableColumn
                       {
                           data ="Service",
                           title = "Сервіс"
                       },
                       new DataTableColumn
                       {
                           data ="CveId",
                           title = "Вразливість"
                       },
                       },
                       Data = Model,
                       Order = new DataTablesOrdering { Column = 0, Type = OrderingType.desc },
                       Searching = false,
                       Selection = new DataTableSelection { Style = SelectionStyle.single }
                   })

</text>, ModalWindowSize.Medium, ModalWindowButtons.OkCancel)
